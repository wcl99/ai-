# 咨询式建单与 CDN 闸门实施计划

1. 在扫描计划下保存咨询消息和结构化需求；CAMEL-AI 只负责补全字段与生成追问。
2. 用只读挂载的 `cdninfo` CLI 检查候选资产；仅 `SAFE` 资产可继续交给小易，失败或不明确均阻断。
3. 在现有 FastAPI 接口中加入咨询与 CDN 检查，不增加服务、队列或常驻 Agent。
4. 将渗透测试首页改为左侧需求结果、右侧固定“专家咨询区”，并展示被 CDN/WAF 阻断的资产。
5. 完成后运行后端测试、迁移检查及前端 lint/test/build，再提交并推送当前分支。
